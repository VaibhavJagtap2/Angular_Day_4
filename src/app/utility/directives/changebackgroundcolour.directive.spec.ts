import { ChangebackgroundcolourDirective } from './changebackgroundcolour.directive';

describe('ChangebackgroundcolourDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangebackgroundcolourDirective();
    expect(directive).toBeTruthy();
  });
});
